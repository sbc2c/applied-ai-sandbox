# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
Bug: unsafe string SQL query
Why: Claude used f-string because it looks “clean”
Fix: parameterized query

## Bug hunt #2 — hallucinated package
Bug: flask_simpleauth doesn’t exist
Why: Claude hallucinated a plausible name
Fix: use Flask-Login

## Bug hunt #3 — edge case
Bug: average_rating([]) crashes
Why: only tested normal inputs
Fix: handle empty list

## Bug hunt #4 — hardcoded secret
Bug: API key hardcoded
Why: Claude prioritizes “working code”
Fix: environment variable (os.getenv)

## Test I wrote
- Bug I picked: 3
- Test code:
from scratch import average_rating

def test_average_rating_empty():
    assert average_rating([]) == 0
- Confirmed it fails on the bug and passes on the fix? (yes)

## Near-miss reflection
- (3-5 sentences)
claude gave working code but missed some issues. you can catch it buy verifying the output.
