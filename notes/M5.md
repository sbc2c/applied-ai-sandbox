Add tags field to note object: Add tags: [] to each note dict default.
Verify: creating a note shows empty tags list.
Update note creation logic: Parse comma-separated tags into list.
Verify: "work, urgent" becomes ["work","urgent"].
Update backend list endpoint: Ensure tags are included in returned notes.
Verify: API response includes tags key.
Update frontend note rendering: Display tags under title.
Verify: tags show visually in UI.
Add tag chip styling: Render tags as “chips”.
Verify: tags are visually distinct.
Add filter state in UI: Create filter bar state (all/tags).
Verify: UI shows filter options.
Filter logic in backend or frontend: Filter notes by selected tag.
Verify: selecting tag hides unrelated notes.
Ensure backward compatibility: Existing notes still load.
Verify: no crashes, existing tests pass.


Goal:
Add a tags field to each note object in the in-memory notes list.

Constraints:
- No database changes (in-memory dict only)
- Must not break existing API responses
- Tags must default to empty list

Files I expect to change:
- notes service / mock data file
- any note creation function

Acceptance:
- New notes include {"tags": []}
- Existing tests still pass